apiVersion: v1
kind: ConfigMap
metadata:
  name: api-platform-config
  namespace: api-platform
  labels:
    app: api-platform
    version: v1
    component: api
data:
  NODE_ENV: "production"
  PORT: "3000"
  LOG_LEVEL: "info"
  DISABLE_REDIS: "false"
